# Database (Docker Compose local)
DATABASE_URL="postgresql://postgres:devpassword@localhost:5432/designpulse_dev"

# Redis (Docker Compose local)
REDIS_URL="redis://localhost:6379"

# Node environment
NODE_ENV="development"

# Anthropic API (Phase 3 — optional for Phase 1)
ANTHROPIC_API_KEY="sk-ant-..."
